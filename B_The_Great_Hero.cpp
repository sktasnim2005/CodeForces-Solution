#include <bits/stdc++.h>
using namespace std;
#define ll long long 
#define ii int
#define ss string
#define dd double
#define bb bool

void solve() {
    
    ll A,B,n;
    cin >> A >> B >> n;

    vector<ll>a(n), b(n);
    for(auto& x : a) cin >> x;
    for(auto& x : b) cin >> x;
    
    
       


}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t; cin >> t; while (t--) 
    solve();
    return 0;
}
